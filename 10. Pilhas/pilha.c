#include <stdio.h>
#include <stdlib.h>

typedef struct pilha Pilha;

Pilha* criaPilha(void);
void pushPilha(Pilha* p, float v);
float popPilha(Pilha* p);
int pilhaVazia(Pilha* p);
void pilhaLibera(Pilha* p);

void main(void){

	return 0;
}